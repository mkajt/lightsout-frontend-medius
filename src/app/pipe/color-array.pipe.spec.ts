import { ColorArrayPipe } from './color-array.pipe';

describe('ColorArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new ColorArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
