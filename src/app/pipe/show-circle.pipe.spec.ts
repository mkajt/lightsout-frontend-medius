import { ShowCirclePipe } from './show-circle.pipe';

describe('ShowCirclePipe', () => {
  it('create an instance', () => {
    const pipe = new ShowCirclePipe();
    expect(pipe).toBeTruthy();
  });
});
